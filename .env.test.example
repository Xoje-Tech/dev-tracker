# Test environment for dev-tracker
# Copy this to .env.test and adjust values for your local setup.
# .env.test is gitignored — do not commit real secrets here.

NODE_ENV=test
PORT=3001
DATABASE_URL="file:./test.db"
SESSION_SECRET="test-only-secret-not-for-production"
