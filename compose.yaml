services:
  dev-tracker-server:
    image: ghcr.io/xoje-tech/dev-tracker-server:latest
    build: .
    ports:
      - "6789:6789"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
