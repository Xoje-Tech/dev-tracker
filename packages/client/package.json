{
  "name": "@dev-tracker/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev Tracker shared client — DevTrackerClient interface, HttpDevTrackerClient, InProcessDevTrackerClient, Zod schemas, errors. Transport-agnostic core consumed by @dev-tracker/cli and @dev-tracker/mcp.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@dev-tracker/backend": "workspace:*",
    "@types/node": "26.0.1",
    "typescript": "^5.9.3",
    "vitest": "4.1.9"
  }
}
