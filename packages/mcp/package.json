{
  "name": "@dev-tracker/mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Model Context Protocol server for dev-tracker — exposes the 20 primitive client methods as MCP tools over stdio.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "dev-tracker-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dev-tracker/client": "workspace:*",
    "@modelcontextprotocol/sdk": "1.29.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  }
}