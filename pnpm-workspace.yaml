# pnpm 11+ per-project settings.
# This file is the new home for pnpm settings (previously nested under `pnpm` in package.json).
# Reference: https://pnpm.io/settings

allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  sqlite3: true

packages:
  - "packages/*"
